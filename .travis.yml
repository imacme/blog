language: node_js
node_js:
  - 12
install:
  - npm install
script:
  - npm run docs:build
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: docs/.vuepress/dist
  target_branch: gh-pages
  keep_history: true
  on:
    branch: master